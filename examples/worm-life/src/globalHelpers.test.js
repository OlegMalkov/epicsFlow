// @flow

import { remember } from "./globalHelpers";
import { type ScriptDataType } from "./models/World";

const initialData: ScriptDataType = 

describe('remember', () => {
	it('work', () => {
		remember()
	})
})